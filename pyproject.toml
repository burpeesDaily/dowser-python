[project]
name = "dowser-python"
version = "0.0.1"
description = "Sensitive data discovery that runs on a laptop or a data lake."
authors = [
    {name="Shun Huang", email="shun@formosa1544.com"}
]
readme = {file = "README.rst", content-type = "text/x-rst"}
requires-python = ">=3.13"
license = {text = "MIT License"}
dependencies = []

[tool.setuptools.packages.find]
exclude = ["tests"]

[project.urls]
repository = "https://github.com/burpeesDaily/dowser-python"
